## 1. Línea base

- [ ] 1.1 Comprobar el entorno de tests (`vendor/`, `wordpress-tests-lib`, base de datos de tests; si falta, `composer install`, `composer run build` y `bash bin/install-wp-tests.sh wordpress_test root root 127.0.0.1:3306 latest`) y verificar que `vendor/bin/phpunit`, `vendor/bin/phpunit -c tests/multisite.xml.dist` y `vendor/bin/phpcs --no-cache` pasan en verde antes de tocar código, anotando el número de tests y comprobando el código de salida de phpcs

## 2. A — Catálogo RFC 9727 completo (A1)

- [ ] 2.1 En `src/Manifest/ManifestBuilder.php::api_catalog()` construir el linkset con dos entradas: `linkset[0]` anclada en `home_url( '/.well-known/api-catalog' )` con `item: [ { href: untrailingslashit( rest_url() ) } ]`, y `linkset[1]` anclada en `untrailingslashit( rest_url() )` con `service-desc` y `service-doc` como hoy; verificar reescribiendo `Test_Agent_Manifest::test_api_catalog_route` (anclas, `item`, `service-desc`, `service-doc` con y sin `auth.md`, `linkset[0]` sin `service-desc`) y con `test_api_catalog_with_plain_permalinks` (`item[0].href` y `linkset[1].anchor` en la forma `?rest_route=`)
- [ ] 2.2 En `src/Manifest/ManifestRouter.php::headers()` enviar `Content-Type: application/linkset+json; profile="https://www.rfc-editor.org/info/rfc9727"` para el catálogo, reescribiendo el comentario sobre RFC 9264/9727; verificar con la aserción de `Content-Type` de `test_api_catalog_route` y con `Test_Diagnostics` (`Report::site_checks()` sigue aceptando el tipo por subcadena: nuevo `test_report_accepts_catalog_with_profile`), y con `vendor/bin/phpcs --no-cache src/Manifest`

## 3. B — Modelo de error tipado en OpenAPI (B1)

- [ ] 3.1 En `src/Manifest/ManifestBuilder.php::openapi()` añadir `components.schemas.Error` (`code` y `message` `string` obligatorios, `data` `object` con `status` `integer` y `additionalProperties`) y, en cada operación, las respuestas `400`, `404` y `default` con `content.application/json.schema.$ref = #/components/schemas/Error` y descripciones traducibles; verificar con `Test_Agent_Manifest::test_openapi_error_schema_and_responses` (esquema, `$ref` en `/wp/v2/posts`, `/wp/v2/posts/{id}`, `/wp/v2/search` y `/wpasl/v1/openapi`) y `test_openapi_has_no_problem_json` (`application/problem+json` ausente en el JSON codificado), manteniendo `test_openapi_document` y `test_openapi_rest_route` en verde

## 4. C — Política de versionado y deprecación (C1)

- [ ] 4.1 Añadir `ManifestBuilder::versioning_policy( array $namespaces )` estático que devuelva el párrafo en inglés de la decisión C1 (sin `__()`), derivar los espacios de nombres de las claves de `paths` (con y sin prefijo `/?rest_route=`) y concatenarlo a `info.description`; verificar con `Test_Agent_Manifest::test_openapi_description_states_versioning_policy` (`wp/v2`, `wpasl/v1`, `Deprecation`, `Sunset`, `changelog`, sin `application/problem+json`) y con un caso de enlaces simples que compruebe que los espacios de nombres se extraen igual
- [ ] 4.2 En `src/Manifest/AuthMdBuilder.php::document()` añadir `## API versioning and deprecation` después de `## Credential use` llamando a `ManifestBuilder::versioning_policy()` con los espacios de nombres derivados de las URLs REST del registro; verificar con `Test_Auth_Md::test_versioning_section` (encabezado, `wp/v2`, `wpasl/v1`, `Deprecation`, `Sunset`, sin `OAuth`) y manteniendo `test_document_structure` y `test_document_never_mentions_oauth_or_application_passwords` en verde

## 5. D — 404 en Markdown para agentes (D1, D2)

- [ ] 5.1 Crear `src/Content/SitemapLocator.php` con `url()` estático que aplique en orden: sitemaps del núcleo (`get_sitemap_url( 'index' )`), líneas `Sitemap:` del `robots.txt` virtual del mismo host, clases de plugins SEO conocidas (verificar en el código de Yoast SEO, Rank Math, AIOSEO y SEOPress los nombres de clase o función y la ruta del índice antes de fijarlos), filtro `wpasl_sitemap_url` documentado, y `null` si nada aplica; hacer que `LlmsTxtBuilder::optional_links()` lo use; verificar con `Test_Llms_Txt::test_optional_sitemap_from_seo_plugin_or_filter` (núcleo deshabilitado por `wp_sitemaps_enabled`, línea `Sitemap:` inyectada por `robots_txt`, y filtro) y `test_optional_omits_sitemap_when_unknown`, manteniendo `test_optional_sitemap_link_uses_query_form_with_plain_permalinks` y `test_structure_with_pages_and_posts` en verde
- [ ] 5.2 En `src/Markdown/Delivery.php` añadir `not_found_document()` (cuerpo en inglés sin `__()` de la decisión D1 con enlaces a `llms.txt`, `auth.md` si `AuthMdBuilder::is_published()`, sitemap si `SitemapLocator::url()` y catálogo si `manifest_enabled`, filtrado por `wpasl_markdown_404` con docblock, sin reflejar la petición) y `maybe_serve_404()` colgado de `template_redirect` prioridad 11 (condiciones: `is_404()` y una de `md_request_post_id === -1`, `wpasl=md`, `prefers_markdown()`), que retire `Link`, dispare `wpasl_before_serve` con contexto `markdown-404`, envíe 404 con `Content-Type: text/markdown; charset=utf-8`, `Vary: Accept`, `X-Markdown-Tokens`, `Cache-Control: no-store`, `X-Content-Type-Options: nosniff` y respete `wpasl_terminate_after_serve`; en `src/Signals/ContentSignals.php::send()` añadir el `Link rel="api-catalog"` también para `markdown-404` y documentar el contexto; verificar con el nuevo `tests/test-markdown-404.php` (`test_md_suffix_for_unknown_content_serves_markdown_404`, `test_md_suffix_for_draft_serves_markdown_404`, `test_accept_markdown_on_unknown_url_serves_markdown_404`, `test_browser_accept_keeps_html_404` (sin salida, `is_404()`), `test_links_follow_configuration` (`auth.md`, sitemap, catálogo y `Link` según ajustes), `test_body_never_reflects_the_request`, `test_headers_and_single_api_catalog_link` (`Content-Signal` presente, `X-Robots-Tag` ausente, un solo `Link`), `test_body_filter`), manteniendo `Test_Delivery::test_md_suffix_for_unknown_content_is_404` y `test_md_suffix_for_home_is_404` en verde

## 6. E — Guía "cuándo usar este sitio" (E1)

- [ ] 6.1 En `src/Settings.php` añadir `llms_when_to_use` (`''`) a `defaults()` y `TAB_KEYS['llms']`, la constante `LLMS_WHEN_TO_USE_MAX = 4000` y su saneado `mb_substr( sanitize_textarea_field(), 0, 4000 )`; en `src/Admin/Tabs/LlmsTab.php` la fila "When to use this site (Markdown)" (`textarea`, `rows="6"`, `maxlength`, descripción de la decisión E1); verificar con `Test_Settings` (default vacío, etiqueta HTML eliminada, 5000 → 4000 idempotente, conservación al guardar la pestaña General) y `Test_Llms_Txt::test_tab_renders_when_to_use_field`
- [ ] 6.2 En `src/Llms/LlmsTxtBuilder.php::build()` emitir `## When to use this site` con el texto tras la introducción cuando no esté vacío, y en `src/Manifest/AuthMdBuilder.php::document()` la misma sección entre `## Audience` y `## Registration and credential provisioning`; verificar con `Test_Llms_Txt::test_when_to_use_section` (posición y ausencia con valor vacío) y `Test_Auth_Md::test_when_to_use_section_present_and_absent`, y que guardar la pestaña llms.txt invalida `auth.md` (`test_settings_change_invalidates_stored_file` ampliado)

## 7. F — llms.txt como índice con archivos por tipo (F1, F2)

- [ ] 7.1 En `src/Settings.php` retirar `llms_limit` de `defaults()`, `TAB_KEYS['llms']` y `sanitize_key_value()`, y añadir `llms_preview_limit` (10, 1..100) y `llms_type_limit` (1000, 1..10000) con saneado que devuelva el default con 0 o vacío y el máximo por encima; en `LlmsTab` sustituir el campo "Items per section" por "Items per section in llms.txt" y "Items per content-type file"; verificar con `Test_Settings` (defaults sin `llms_limit`, rangos 500 → 100 y 50000 → 10000, defaults con 0, idempotencia, conservación al guardar otra pestaña, opción almacenada con `llms_limit => 100` que no falla ni influye)
- [ ] 7.2 En `src/Llms/LlmsTxtBuilder.php` añadir `RESERVED_TYPES = ['full']`, `RECOMMENDED_MAX_CHARS = 30000`, `type_file( $type )`, `type_files()`, `preview_sections( $sections )` (corte a `llms_preview_limit`, filtro `wpasl_llms_preview_sections` documentado) y `build_type( $type, $ids )` (H1 `# <sitio> — <etiqueta>`, blockquote con `n` ítems y enlace a `llms.txt`, líneas `item_line()`, nota `> Truncated: listing <limit> of <total> items.` cuando `Eligibility::count( [ $type ] )` supera el límite); hacer que `sections()` use `llms_type_limit`, que `build()` use la vista previa y cierre cada sección con `- [Full list of <etiqueta> (<n> items)](<url>)` cuando `count( $ids ) > llms_preview_limit` y el tipo no esté reservado, y que `build_full()` use las listas completas; `generate()` escribe `llms.txt`, cada archivo por tipo y `llms-full.txt`, y borra los `llms-*.txt` de tipos no habilitados; `generate_file()` acepta nombres por tipo; verificar con `Test_Llms_Txt::test_preview_and_full_list_line` (10 de 25 + línea con `(25 items)`; 3 páginas sin línea), `test_type_file_lists_all_items_and_truncates` (150 de 150; 20 de 30 con nota y `(20 items)` en `llms.txt`), `test_reserved_type_full`, `test_llms_full_concatenates_full_lists` (vista previa 2, 5 documentos) y manteniendo `test_limit_keeps_the_most_recent_posts` (adaptado a `llms_type_limit`), `test_pages_are_ordered_by_menu_order_then_title`, `test_hierarchical_cpt_is_ordered_by_date` y `test_excluded_and_non_eligible_items_are_not_listed` (ampliado al archivo por tipo) en verde
- [ ] 7.3 En `src/Llms/LlmsTxtRouter.php` hacer que `requested_file()` reconozca `llms-<tipo>.txt` solo para post types habilitados con archivo, `physical_path( $file = 'llms.txt' )` con el nombre como segundo argumento del filtro `wpasl_physical_llms_path` (docblock actualizado), `physical_file_exists( $file )`, `handle_request()` con precedencia del físico para todos los archivos, `document()` con generación bajo demanda de `llms.txt` y de los archivos por tipo (nunca `llms-full.txt`), e `invalidate()` que borre `llms.txt`, `llms-full.txt` y todo `llms-*.txt` del almacenamiento; verificar con `Test_Llms_Txt::test_type_file_route_headers_and_lazy_generation` (200, cabeceras iguales a `llms.txt`, `Content-Signal`, solo ese archivo escrito, segunda petición sin regenerar), `test_type_file_unknown_or_disabled_type_is_404` (`/llms-attachment.txt`, `/llms-nada.txt`, tipo deshabilitado), `test_type_file_non_canonical_paths`, `test_type_file_physical_precedence`, `test_invalidation_and_regeneration_remove_stale_type_files` (guardar ajustes borra; ciclo regenera; tipo deshabilitado desaparece) y manteniendo `test_llms_txt_request_does_not_build_llms_full`, `test_missing_llms_full_returns_503_and_schedules_build` y `test_settings_change_invalidates_stored_files` en verde
- [ ] 7.4 Extender `Test_Llms_Txt::test_every_emitted_markdown_url_is_servable` para recorrer también las URLs Markdown de cada `build_type()` y servir por el router cada `/llms-<tipo>.txt` enlazado desde `build()` (añadiendo ítems por encima de la vista previa para que aparezca la línea "Full list"); verificar que pasa junto con `Test_Delivery` completo
- [ ] 7.5 En `src/Llms/LlmsTxtRouter.php::register_tab()` pasar `Storage` a `LlmsTab` y en `src/Admin/Tabs/LlmsTab.php` mostrar las URLs de los archivos por tipo de los tipos habilitados, el tamaño en caracteres del `llms.txt` almacenado (o "not generated yet") y el aviso `notice notice-warning inline` por encima de `RECOMMENDED_MAX_CHARS` recomendando bajar la vista previa; verificar con `Test_Llms_Txt::test_tab_shows_size_and_warning` (82.000 → aviso; 20.000 → sin aviso; sin archivo → texto de no generado; URLs `/llms-page.txt` y `/llms-post.txt`) y manteniendo `test_tab_renders_and_sanitizes_mb` en verde
- [ ] 7.6 En `src/Diagnostics/CrawlerProbe.php` inyectar `LlmsTxtBuilder` (cablear en `src/Plugin.php`), añadir a `site_targets()` una clave `llms-<tipo>` por archivo por tipo entre `llms` y `auth`, guardar `body_length` en `fetch()` para los objetivos con cuerpo, medido antes del recorte a `MAX_BODY_KEPT` (64 KiB; `MAX_RESPONSE_BYTES` es 1 MiB y no cambia); en `src/Diagnostics/Report.php::site_checks()` aceptar dinámicamente las claves `llms-*` con `text/markdown` y etiqueta `llms-<tipo>.txt`, y marcar `llms` como advertencia cuando `body_length > 30000` con el mensaje de la decisión F2; en `src/Admin/Tabs/DiagnosticsTab.php` añadir los archivos por tipo a `curl_commands()` y al checklist; verificar con `Test_Diagnostics::test_report_checks_type_files` (200 → correcto, 404 → error, orden de claves), `test_report_warns_on_large_llms_txt` (82.000 → advertencia; 20.000 → correcto), ampliación de `test_tab_renders_checklist_and_curl_commands` (`curl -s '<home_url/llms-page.txt>'`), actualización de la lista exacta de claves en `test_report_checks_auth_md` y `test_probe_only_sends_get_to_known_targets`, y manteniendo `test_tab_renders_report_from_previous_version_without_notices` en verde con un informe sin claves `llms-*`

## 8. G — Enlaces de descubrimiento en el head y shortcode (G1)

- [ ] 8.1 Crear `src/Manifest/DiscoveryLinks.php` con `links()` (lista `[ rel, type, href, label ]` de la decisión G1, vacía sin `manifest_enabled`, `auth.md` solo si `AuthMdBuilder::is_published()`, filtro `wpasl_discovery_links` documentado), `print_links()` en `wp_head` prioridad 5 (`<link rel="…" type="…" href="…" />` con `esc_attr`/`esc_url`, nunca en la administración) y el shortcode `wpasl_agent_links` registrado en `init` (`<ul class="wpasl-agent-links">` con `<a href rel type>` y etiquetas traducibles, `''` sin manifiestos); cablear el servicio `discovery_links` en `src/Plugin.php` tras `auth_md_router`; verificar con el nuevo `tests/test-discovery-links.php` (`test_head_links_on_front_page`, `test_head_links_on_singular_post_alongside_alternate`, `test_no_auth_md_link_when_unpublished`, `test_nothing_when_manifests_disabled` (head vacío y shortcode `''`), `test_shortcode_renders_list` (`do_shortcode`, cuatro `href` escapados, clase), `test_links_filter`) y con `vendor/bin/phpcs --no-cache src/Manifest/DiscoveryLinks.php tests/test-discovery-links.php`

## 9. Calidad y cobertura

- [ ] 9.1 Revisar que todas las cadenas de interfaz nuevas están en inglés con el text domain `wp-agent-support-layer` y comentarios `translators:` donde haya placeholders, que los textos para agentes (`auth.md`, 404 Markdown, archivos por tipo, política de versionado, encabezado `## When to use this site`) no pasan por `__()`, que los filtros nuevos (`wpasl_markdown_404`, `wpasl_sitemap_url`, `wpasl_discovery_links`, `wpasl_llms_preview_sections`) y el argumento nuevo de `wpasl_physical_llms_path` tienen docblock, y ejecutar `vendor/bin/phpcs --no-cache` sobre `src/` y `tests/` con código de salida 0
- [ ] 9.2 En `docs/spec-coverage.md` citar `agentic-readiness-round-1` en el párrafo introductorio y añadir una fila por escenario de los cinco deltas (`agent-manifest`: catálogo disponible, ruta no canónica, `auth.md` en el catálogo y desactivado, enlaces simples del catálogo, documento válido, solo GET, enlaces simples del OpenAPI, modelo de error, sin RFC 9457, política en la descripción, estructura de `auth.md`, guía en `auth.md`, versionado, enlaces en portada, página interior, `auth.md` desactivado, manifiestos deshabilitados, shortcode; `llms-txt`: guía, vista previa con enlace, tipo con menos ítems, sitemap SEO, sitemap indeterminable, más ítems que la vista previa, más ítems que el límite por tipo, concatena listas completas, y todos los escenarios de los archivos por tipo; `markdown-delivery`: los ocho escenarios del 404 Markdown; `admin-settings`: los ocho escenarios de la pestaña llms.txt; `agent-diagnostics`: archivos por tipo, `llms.txt` grande, catálogo con `profile`, comandos para los archivos por tipo) apuntando a los tests de las tareas 2 a 8; verificar que cada escenario de los cinco deltas tiene fila

## 10. Verificación final

- [ ] 10.1 Ejecutar `vendor/bin/phpunit` y `vendor/bin/phpunit -c tests/multisite.xml.dist` y verificar que pasan en verde con los tests nuevos contados respecto a 1.1
- [ ] 10.2 Ejecutar `vendor/bin/phpcs --no-cache` sobre todo el proyecto y verificar código de salida 0
- [ ] 10.3 Ejecutar `openspec validate agentic-readiness-round-1 --strict` sin errores y confirmar con `git diff --stat` que `readme.txt`, `README.md`, `languages/` y la versión del plugin no cambiaron
- [ ] 10.4 Comprobar de extremo a extremo en un WordPress local que `curl -si <sitio>/.well-known/api-catalog` devuelve el `profile` y `linkset[0].item`, que el OpenAPI contiene `components.schemas.Error` y la política de versionado, que `curl -si <sitio>/no-existe.md` devuelve 404 `text/markdown` con los enlaces, que `curl -s <sitio>/llms.txt` pesa menos de 30.000 caracteres y enlaza `/llms-post.txt` (que responde 200 con la lista completa), que `/auth.md` contiene las dos secciones nuevas, y que la portada contiene los cuatro `<link>` de descubrimiento y `[wpasl_agent_links]` imprime la lista; anotar la evidencia para la release
