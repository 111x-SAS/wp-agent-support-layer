## 1. Línea base

- [x] 1.1 Comprobar el entorno de tests (`vendor/`, `vendor-prefixed/` vía `composer run build`, `wordpress-tests-lib`, base de datos de tests; si falta, `composer install`, `composer run build` y `bash bin/install-wp-tests.sh wordpress_test root root 127.0.0.1:3306 latest`) y verificar que `vendor/bin/phpunit`, `vendor/bin/phpunit -c tests/multisite.xml.dist` y `vendor/bin/phpcs --no-cache` pasan en verde antes de tocar código, anotando el número de tests y comprobando el código de salida de phpcs
- [x] 1.2 Verificar en el código o la documentación de cada constructor las claves de meta y valores de la decisión A1 (Elementor `_elementor_edit_mode`/`_elementor_data`, Divi `_et_pb_use_builder`, Beaver Builder `_fl_builder_enabled`, Bricks `_bricks_editor_mode`/`_bricks_page_content_2`, Oxygen `ct_builder_shortcodes`/`ct_builder_json`, Breakdance `breakdance_data`) y dejar la lista definitiva con la fuente consultada en un comentario de `ContentSource::BUILDER_META`; verificar que cada entrada tiene predicado (valor exacto o "no vacío") y meta adicional cuando aplique

## 2. C — Extracción de la región de contenido (C1)

- [x] 2.1 Crear `src/Markdown/ContentExtractor.php` con `to_xpath( $selector )` estático (gramática de la decisión C1: etiqueta, `#id`, `.clase`, `[attr]`, `[attr="v"]`/`[attr='v']`, combinadores descendiente e hijo, listas por comas; `null` ante cualquier otra sintaxis; valores escapados con `concat()`); verificar con el nuevo `tests/test-content-extractor.php::test_selector_to_xpath` (tabla `main`, `div.entry-content > .inner`, `#content article`, `[role="main"]`, `.elementor[data-elementor-type]`, `a, b`) y `test_invalid_selectors_are_rejected` (`div:has(p)`, `a::before`, `a + b`, `[x^="y"]`, cadena vacía → `null`)
- [x] 2.2 Implementar `extract( $html, $post )` y `find_region( $html, $selector = '' )` (carga DOM con errores silenciados como `LeagueConverter::clean()`, selector configurado filtrado por `wpasl_content_selector` y luego la lista `wpasl_rendered_content_selectors`, coincidencia de mayor texto tras la limpieza, `body` como último recurso, eliminación de la lista de nodos y de `wpasl_rendered_remove_selectors`, supresión del primer `h1` que coincida con el título normalizado, fragmento de salida, `no_content` cuando no queda texto), con docblocks de los tres filtros; crear las fixtures `tests/fixtures/rendered-elementor.html` (header/footer del tema, `.elementor-location-header`, `main > div.elementor[data-elementor-type="wp-page"]` con widgets, `h1` con el título), `tests/fixtures/rendered-classic.html` (`main#primary > article.post` y `aside#secondary`) y `tests/fixtures/rendered-no-region.html` (`body` con párrafos y `nav`); verificar con `test_elementor_page_keeps_widgets_and_drops_chrome`, `test_classic_theme_uses_article_not_aside`, `test_without_region_falls_back_to_body_without_nav`, `test_removed_elements_do_not_leak` (script, style, form, button, `hidden`, `aria-hidden`, `screen-reader-text`), `test_duplicate_h1_is_removed_once`, `test_configured_selector_wins_and_falls_back_when_missing`, `test_selector_list_filter`, `test_no_content_is_reported` y `vendor/bin/phpcs --no-cache src/Markdown/ContentExtractor.php tests/test-content-extractor.php`

## 3. A — Ajustes, meta box y reinicio de la cola (A3, A4)

- [x] 3.1 En `src/Settings.php` añadir `content_source` (`array()`) y `content_selector` (`''`) a `defaults()` y a `TAB_KEYS['general']`, con saneado de la decisión A4 (`content_source` solo conserva `editor`/`rendered` por tipo seleccionable; `content_selector` recortado y validado con `ContentExtractor::to_xpath()`, y ante un selector inválido conserva el valor almacenado y registra `add_settings_error()` con el subconjunto admitido); verificar con `Test_Settings::test_content_source_and_selector_defaults_and_sanitization` (defaults, `foo` → ausente, `rendered` conservado, selector válido idempotente, selector inválido conserva `main` y registra el aviso) y ampliando `test_sanitize_is_idempotent_for_every_field` y `test_saving_another_tab_keeps_auth_md_settings` (guardar Señales conserva origen y selector)
- [x] 3.2 En `src/Admin/Tabs/GeneralTab.php` renderizar el `<select>` "Content source" por tipo habilitado (`auto`/`editor`/`rendered` con la descripción de la decisión A4) y la fila "Content selector (CSS)"; verificar con `Test_Settings::test_general_tab_renders_content_source_and_selector` (un `select` por tipo habilitado con `auto` seleccionado por defecto, `rendered` seleccionado cuando está almacenado, campo de texto con el valor almacenado) y manteniendo `test_general_tab_renders_status_form_outside_settings_form` en verde
- [x] 3.3 En `src/Admin/ExcludeMetaBox.php` añadir `SOURCE_META = '_wpasl_content_source'`, su registro por tipo habilitado (`string`, `single`, `default ''`, `show_in_rest`, `sanitize_callback` a `editor`/`rendered`/`''`, mismo `auth_callback`), la ocultación a lectores sin permiso de ambas claves, el `<select>` "Markdown content source" en `render()` y su guardado en `save()` (vacío → `delete_post_meta()`); verificar con `Test_Exclude_Meta_Box::test_source_meta_is_registered_only_for_enabled_post_types`, `test_save_stores_source_override_and_clears_default`, `test_source_meta_hidden_for_anonymous_and_writable_by_editor` (lectura anónima sin la clave, escritura REST de `rendered` almacenada y de `foo` saneada a vacío) y `test_render_shows_select_only_for_enabled_types`
- [x] 3.4 En `src/Generation/Scheduler.php::on_settings_updated()` descartar la cola cuando cambien `content_source` (normalizado) o `content_selector`, sin tocar `generated`; verificar con `Test_Scheduler::test_changing_content_source_or_selector_clears_queue_and_keeps_generated` y `test_saving_general_without_changes_keeps_queue`

## 4. A — Resolución del origen (A1)

- [x] 4.1 Crear `src/Markdown/ContentSource.php` con `resolve( \WP_Post $post )` que evalúe en orden la anulación por entrada, el ajuste por tipo, `get_page_template_slug()`, la lista `BUILDER_META` (filtro `wpasl_builder_meta_keys`), el archivo de plantilla (`template_candidates()` filtrable por `wpasl_template_candidates`, `locate_template()` filtrable por `wpasl_template_file`, lectura acotada a 256 KB, predicado `the_content`/`wp:post-content`, inconcluyente ante `get_template_part(`/`locate_template(`/`include` dinámicos, seguimiento de `get_template_part` literal un nivel, caché estática por `ruta:mtime`, rama `resolve_block_template()` en temas de bloques) y la red final `empty_editor` con `wpasl_editor_min_chars` (100) usando un callable de conversión inyectado, devolviendo `array( 'source', 'reason' )` filtrado por `wpasl_content_source`, con docblocks; verificar con el nuevo `tests/test-content-source.php`: `test_post_override_wins`, `test_post_type_setting_wins_over_auto_conditions`, `test_assigned_page_template_triggers_rendered` (`_wp_page_template` = `templates/landing.php` vs `default`), `test_builder_meta_triggers_rendered` (Elementor completo, Elementor sin `_elementor_data`, clave añadida por filtro), `test_template_file_without_the_content_triggers_rendered` (archivo temporal vía `wpasl_template_file`), `test_template_file_with_the_content_or_dynamic_part_is_inconclusive`, `test_template_part_literal_is_followed_one_level`, `test_empty_editor_triggers_rendered` (contenido vacío y contenido corto bajo el umbral, umbral por filtro), `test_normal_content_defaults_to_editor`, `test_resolution_filter`, `test_template_cache_keyed_by_mtime`
- [x] 4.2 Añadir la rama de temas de bloques con `wp_is_block_theme()` + `resolve_block_template()` sobre los mismos candidatos sin `.php`, contando `wp:post-content` como impresión del editor y `wp:template-part` como inconcluyente; verificar con `Test_Content_Source::test_block_theme_template_without_post_content_triggers_rendered` usando el tema `block-theme` de los datos de la suite de WordPress si está disponible (`switch_theme()`), y en su defecto marcando el test como omitido con motivo explícito

## 5. B — Loopback y marca de renderizado (B1, B2)

- [x] 5.1 Crear `src/Markdown/RenderedPage.php` con `url( $post )` (`add_query_arg( 'wpasl_render', '1', get_permalink() )`), `fetch( $post )` (rechazo de host distinto de `home_url()`, argumentos por defecto de la decisión B1 filtrados por `wpasl_render_request_args`, cabecera `X-WPASL-Render: 1`, `reject_unsafe_urls`, seguimiento manual de hasta dos redirecciones al mismo host, motivos `external_host`, `request_error:*`, `http_<código>`, `redirect_external_host`, `redirect_loop`, `not_html`, `empty_body`), y la deadline (`wpasl_run_started`/`wpasl_run_finished`, `MIN_TIMEOUT = 2`, resultado `deferred`); verificar con el nuevo `tests/test-rendered-page.php` (loopback simulado con `pre_http_request` a prioridad 20 que registra los argumentos): `test_request_carries_marker_accept_timeout_and_size`, `test_args_filter_changes_timeout`, `test_external_host_is_never_requested`, `test_same_host_redirect_is_followed_at_most_twice`, `test_external_redirect_fails_without_contacting_host`, `test_http_errors_and_wp_error_fail_with_reason`, `test_non_html_content_type_fails`, `test_deadline_caps_timeout_and_defers_below_minimum`, y `vendor/bin/phpcs --no-cache src/Markdown/RenderedPage.php tests/test-rendered-page.php`
- [x] 5.2 En `src/Markdown/Delivery.php` añadir `is_render_request()` (cabecera `X-WPASL-Render` o parámetro `wpasl_render`, saneados) y salir sin servir ni generar en `handle_md_suffix()`, `maybe_serve()` y `maybe_serve_404()` cuando sea verdadero, documentando que una petición de renderizado nunca recibe Markdown; verificar con `Test_Delivery::test_render_request_never_gets_markdown_nor_generates` (cabecera + `Accept: text/markdown` → sin salida, sin archivo en el almacenamiento, sin marca en el estado; parámetro + `Accept: text/markdown` igual) y `test_render_request_on_static_front_page_resolves_front_page` (`go_to( '/?wpasl_render=1' )` con portada estática → `is_front_page()` y `is_page()`)

## 6. B/D — DocumentBuilder integrado y cableado (A2, D front matter)

- [x] 6.1 En `src/Markdown/DocumentBuilder.php` aceptar `ContentSource`, `RenderedPage` y `ContentExtractor` opcionales, resolver el origen, obtener y extraer cuando sea `rendered`, degradar al editor con `fallback`/`error`, reutilizar el cuerpo del editor calculado por la red final, devolver `null` con `deferred`, añadir `source` al front matter después de `lang`, y disparar `wpasl_content_source_resolved` con docblock; cablear las tres instancias en `src/Plugin.php` (servicios `content_source`, `rendered_page`, `content_extractor`); verificar con `Test_Document_Builder::test_front_matter_has_source_editor_by_default` (ampliando `test_front_matter_contains_required_keys_and_taxonomies` con `source: "editor"` tras `lang`), `test_rendered_source_uses_loopback_body` (`pre_http_request` devuelve la fixture de Elementor para una entrada con meta de Elementor → cuerpo con los widgets, `source: "rendered"`, una sola petición), `test_loopback_failure_falls_back_to_editor_and_reports` (503 → `source: "editor"`, cuerpo del editor, acción con `fallback` y `error` `http_503`), `test_no_content_region_falls_back` (HTML sin texto → `no_content`), `test_html_filter_applies_to_rendered_fragment`, `test_builder_without_services_behaves_as_before` (instancia con solo el conversor), `test_document_is_identical_from_cron_and_from_singular_request` ampliado a un ítem `rendered`, y manteniendo `test_building_documents_restores_all_post_globals` en verde
- [x] 6.2 Verificar la generación bajo demanda con loopback en `Test_Delivery::test_lazy_fill_of_rendered_item_does_loopback_in_request` (sin archivo, entrada con meta de Elementor, `pre_http_request` con la fixture → 200 con `source: "rendered"`, archivo almacenado, una petición) y `test_lazy_fill_serves_editor_when_loopback_fails` (timeout simulado → 200 con `source: "editor"`, archivo almacenado, fallo de renderizado registrado en el estado)

## 7. B — Runner y estado: fallos, diferido, reintento y panel (B3, B4)

- [x] 7.1 En `src/Generation/State.php` añadir `render_failed` y `last_render_error` a `defaults()`, `record_render_failure( $post_id, $reason )`, `clear_render_failure( $post_id )` y la fusión en `save()` (más reciente gana; `_removed` los elimina); verificar con `Test_State::test_render_failures_are_recorded_cleared_and_merged`
- [x] 7.2 En `src/Generation/Runner.php` disparar `wpasl_run_started( $deadline )`/`wpasl_run_finished` en `run()`, escuchar `wpasl_content_source_resolved` (acumular en `run_render_marks` dentro de la ejecución o escribir el estado fuera; emitir `wpasl_render_failed` con docblock y la línea del error log; limpiar el contador cuando `rendered` sin fallback), tratar `deferred` (el id vuelve al frente, no cuenta como procesado ni fallo, la ejecución termina), anteponer tras los nunca generados los ids de `render_failed` con contador bajo `max_failures()`, intersecar `render_failed` con los elegibles al completar el ciclo, y añadir `render_failed` y `last_render_error` a `status()`; verificar con `Test_Runner::test_render_failure_is_recorded_without_counting_as_conversion_failure` (documento almacenado y marcado, `failed` sin cambios, `render_failed` = 1, acción y error log), `test_render_success_clears_counter`, `test_render_failed_items_go_first_after_fresh_ones`, `test_render_failed_items_at_threshold_keep_normal_order`, `test_deferred_item_returns_to_front_and_run_ends`, `test_run_started_and_finished_actions_carry_deadline`, `test_on_demand_render_failure_during_run_is_kept_in_saved_state`, `test_completed_cycle_prunes_render_failed_of_ineligible_items`, y manteniendo `test_failed_items_are_counted_and_deprioritized`, `test_time_budget_stops_the_run_and_the_next_run_continues` y `test_run_with_full_llms_writes_state_once` en verde
- [x] 7.3 En `src/Admin/GenerationStatus.php` añadir las filas "Items with a rendered-page failure" y "Last rendered-page failure" (motivo, id y fecha, o "None") y el aviso `notice-warning inline` que remite a la pestaña Diagnóstico cuando hay fallos; verificar con `Test_Generation_Status::test_render_failures_row_and_notice` (con 3 fallos y motivo `http_403` → "3", el motivo, la fecha y el aviso; sin fallos → "0" y sin aviso)

## 8. D — WP-CLI (D1)

- [x] 8.1 En `src/CLI/Commands.php` añadir a `status` las filas `render_failed` y `last_render_error`, y el subcomando `source <id>` (error con la razón de no elegibilidad o id inexistente; salida `source`, `reason`, `selector` con `--format`); inyectar `ContentSource` y `Settings` en el constructor y cablear en `src/Plugin.php`; verificar con `Test_CLI::test_status_shows_render_failures` (fallo `timeout` registrado → filas presentes) y `test_source_command_reports_resolution_and_errors` (entrada con meta de Elementor → `rendered`/`builder:elementor`; id inexistente → error; entrada en borrador → error con `status`)

## 9. D — Diagnóstico (D2)

- [x] 9.1 En `src/Diagnostics/CrawlerProbe.php` añadir el objetivo `render` a `site_targets()` (tras `markdown_url`, solo con entrada de muestra), el parámetro `$headers` extra y el callable `$analyze` en `fetch()`, y en `probe_site()` pedir `render` con `Accept: text/html`, `X-WPASL-Render: 1` y análisis con `ContentExtractor::find_region()` que guarde `content_region`; en `src/Diagnostics/Report.php::site_checks()` la comprobación `render` con los mensajes de la decisión D2; verificar con `Test_Diagnostics::test_render_target_is_probed_with_marker_and_html_accept`, `test_report_render_check_ok_with_region` (200 + `main` → correcta con `main`), `test_report_render_check_warns_without_region`, `test_report_render_check_errors_on_block_markdown_or_connection_error` (403, `text/markdown`, `WP_Error`), la actualización de la lista exacta de claves en `test_report_checks_auth_md` y `test_probe_only_sends_get_to_known_targets`, y manteniendo `test_tab_renders_report_from_previous_version_without_notices` en verde con un informe sin `render`
- [x] 9.2 En `src/Admin/Tabs/DiagnosticsTab.php` añadir el aviso de fallos de renderizado (lee `Runner::status()`; inyectar `Runner` y cablear en `src/Plugin.php`), la línea del checklist sobre loopback y el comando `curl -s -H 'X-WPASL-Render: 1' '<url>'` en `curl_commands()`; verificar con `Test_Diagnostics::test_tab_shows_render_failures_notice_and_hides_it_without_failures` y ampliando `test_tab_renders_checklist_and_curl_commands` y `test_curl_commands_are_shell_safe` con el comando de renderizado

## 10. Calidad y cobertura

- [ ] 10.1 Revisar que todas las cadenas de interfaz nuevas están en inglés con el text domain `wp-agent-support-layer` y comentarios `translators:` donde haya placeholders, que los filtros y acciones nuevos (`wpasl_content_source`, `wpasl_builder_meta_keys`, `wpasl_template_candidates`, `wpasl_template_file`, `wpasl_editor_min_chars`, `wpasl_render_request_args`, `wpasl_rendered_content_selectors`, `wpasl_rendered_remove_selectors`, `wpasl_content_selector`, `wpasl_content_source_resolved`, `wpasl_render_failed`, `wpasl_run_started`, `wpasl_run_finished`) tienen docblock, y ejecutar `vendor/bin/phpcs --no-cache` sobre `src/` y `tests/` con código de salida 0
- [ ] 10.2 En `docs/spec-coverage.md` citar `rendered-content-source` en el párrafo introductorio y añadir una fila por escenario de los cuatro deltas (`markdown-delivery`: clave `source`, filtros compartidos, los nueve escenarios del origen, los ocho del loopback y los siete de la extracción; `scheduled-generation`: loopback acotado, ítem diferido, reintento, ítems con fallo de renderizado, estado y origen por WP-CLI, origen o selector cambiados, los cuatro de fallos registrados; `admin-settings`: origen por defecto, guardar origen y selector, selector rechazado, guardar otra pestaña, los cuatro del origen por entrada; `agent-diagnostics`: los tres de la URL de renderizado, el comando `curl`, el aviso con y sin fallos) apuntando a los tests de las tareas 2 a 9; verificar que cada escenario de los cuatro deltas tiene fila

## 11. Verificación final

- [ ] 11.1 Ejecutar `vendor/bin/phpunit` y `vendor/bin/phpunit -c tests/multisite.xml.dist` y verificar que pasan en verde con los tests nuevos contados respecto a 1.1
- [ ] 11.2 Ejecutar `vendor/bin/phpcs --no-cache` sobre todo el proyecto y verificar código de salida 0
- [ ] 11.3 Ejecutar `openspec validate rendered-content-source --strict` sin errores y confirmar con `git diff --stat` que `readme.txt`, `README.md`, `languages/` y la versión del plugin no cambiaron
- [ ] 11.4 Comprobar de extremo a extremo en un WordPress local con Elementor (o con un `single-<tipo>.php` de contenido fijo) que `wp wpasl source <id>` devuelve `rendered` con su razón, que `curl -s '<permalink>.md'` devuelve `source: "rendered"` y el texto de la página, que `curl -s -H 'X-WPASL-Render: 1' '<permalink>?wpasl_render=1' -H 'Accept: text/markdown'` devuelve HTML, que con el loopback bloqueado (por ejemplo `pre_http_request` en un mu-plugin) el `.md` sale con `source: "editor"` y la pestaña General y Diagnóstico muestran el aviso, y que la comprobación "Rendered page" del diagnóstico indica el selector; anotar la evidencia para la release
