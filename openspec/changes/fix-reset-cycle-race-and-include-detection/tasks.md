## 1. Carrera en la regeneración completa restringida a un tipo

- [ ] 1.1 En `src/Generation/Runner.php::reset_cycle()`, dejar la rama `null === $post_types` tal cual (vaciar `generated`, `failed` y `queue` y guardar con `save( $state, false )`, ya sea con un return temprano o con el if/else actual) y reescribir solo la rama restringida a post types: seguir calculando `$ids = $this->eligibility->eligible_ids( $post_types )` (int[]), seguir quitando esos ids de `failed` con `array_diff_key( (array) $state['failed'], array_fill_keys( $ids, true ) )` sobre el estado cargado (save() nunca fusiona `failed` ni `queue`), y para `generated` sustituir el diff local más guardado con reemplazo por `$state = State::narrowed( $state )`, `$state['_removed'] = $ids` y `$this->state->save( $state )` con la fusión por defecto, exactamente como hacen `prune()` (rama de autopersistencia) y el final de `run()`. Verificar con `php -l src/Generation/Runner.php`.
- [ ] 1.2 Añadir sobre la nueva rama un comentario análogo a los de `prune()`/`run()` que explique por qué solo se pasan los ids tocados vía `_removed` (la fusión de `save()` reconstruye el resto desde una relectura sin caché, así que una marca registrada concurrentemente para una entrada de otro tipo no se descarta). Verificar releyendo el diff que el comentario existe y que `queue` sigue quedando vacía en ambas ramas.
- [ ] 1.3 En `tests/test-runner.php`, junto a `test_reset_cycle_without_types_forgets_everything`, añadir un test de regresión con el mismo patrón de caché envenenada que `test_prune_self_persist_does_not_resurrect_a_concurrently_forgotten_item`: crear una entrada de tipo `page` y ejecutar `run_cycle()` para que quede marcada; cargar el estado en `$before`; crear una entrada de tipo `post` nueva y marcarla concurrentemente con `( new State() )->mark_generated( $id )`; `wp_cache_set( State::OPTION, $before, 'options' )`; llamar `$this->runner->reset_cycle( array( 'page' ) )`; y afirmar sobre `( new State() )->load()` que la `page` ya no está en `generated`, que el `post` marcado concurrentemente SÍ sigue en `generated` y que `queue` es `array()`. Verificar que el test falla contra el código anterior (revirtiendo temporalmente 1.1) y pasa con la corrección.
- [ ] 1.4 Ejecutar `vendor/bin/phpunit --filter 'reset_cycle|prune_self_persist|concurrent'` y confirmar que `test_reset_cycle_replaces_instead_of_merging` (`tests/test-state.php`) y `test_reset_cycle_without_types_forgets_everything` (`tests/test-runner.php`) pasan sin modificarse.

## 2. Detección de include/require en archivos de plantilla

- [ ] 2.1 En `src/Markdown/ContentSource.php::analyze_template()`, cambiar la expresión regular de detección a `'/(?<!["\'])\b(?:include|require)(?:_once)?(?!\w)\s*\(?\s*([^;]+);/i'` (lookahead negativo `(?!\w)` justo después del grupo opcional `(?:_once)?`) y ampliar el comentario que la precede para explicar que la palabra clave, con o sin `_once`, no puede ir seguida de otro carácter de palabra: un identificador más largo que meramente empiece por ella (`$include_path`, un método `require_once_algo()`) no es la construcción del lenguaje. Verificar con `php -l src/Markdown/ContentSource.php`.
- [ ] 2.2 En `tests/test-content-source.php`, junto a `test_include_as_array_key_does_not_look_like_a_dynamic_include`, añadir un test con `normal_post()` y `use_template( 'single-post.php', ... )` que combine copia de marketing fija con `$include_path = get_stylesheet_directory();` y afirme `resolve()` igual a `array( 'source' => 'rendered', 'reason' => 'template_file:single-post.php' )`; después `ContentSource::flush_template_cache()` y una segunda plantilla con `$this->require_once_something( $file );` (o similar) con la misma aserción; y, en el mismo test, una tercera plantilla que además contenga un `include $file;` real, afirmando `array( 'source' => 'editor', 'reason' => 'default' )` para demostrar que el include dinámico sigue detectándose. Verificar que el test falla contra la regex anterior y pasa con la nueva.
- [ ] 2.3 Ejecutar `vendor/bin/phpunit --filter ContentSource` (o el nombre de la clase de `tests/test-content-source.php`) y confirmar que todos los tests existentes del archivo, incluidos `test_include_as_array_key_does_not_look_like_a_dynamic_include` y los casos `dynamic include`/`dynamic require_once` de `test_template_file_with_the_content_or_dynamic_part_is_inconclusive`, pasan sin modificarse.

## 3. Verificación global

- [ ] 3.1 Ejecutar `vendor/bin/phpunit` y `vendor/bin/phpunit -c tests/multisite.xml.dist` y verificar que ambas suites pasan sin fallos ni riesgos nuevos.
- [ ] 3.2 Ejecutar `vendor/bin/phpcs --no-cache src/Generation/Runner.php src/Markdown/ContentSource.php tests/test-runner.php tests/test-content-source.php` y verificar por el código de salida que no reporta incidencias nuevas.
- [ ] 3.3 Ejecutar `openspec validate fix-reset-cycle-race-and-include-detection --strict` y verificar que la propuesta y los delta specs siguen siendo válidos antes de abrir el PR.
