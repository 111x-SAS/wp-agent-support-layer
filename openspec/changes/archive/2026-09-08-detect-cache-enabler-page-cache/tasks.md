## 1. Línea base

- [x] 1.1 Comprobar el entorno de tests (`vendor/`, `wordpress-tests-lib`, contenedor `wpasl-mysql`; si falta, `composer install`, `composer run build` y `bash bin/install-wp-tests.sh wordpress_test root root 127.0.0.1:3306 latest`) y verificar que `vendor/bin/phpunit` y `vendor/bin/phpcs --no-cache` pasan en verde antes de tocar código, anotando el número de tests

## 2. Detección local y valores a replicar (D1, D3, D5)

- [x] 2.1 Añadir `api_catalog_link()` público a `src/Signals/ContentSignals.php` que devuelva `'<' . home_url( '/.well-known/api-catalog' ) . '>; rel="api-catalog"'` y hacer que `send()` lo use; verificar que `Test_Content_Signals` y `Test_Delivery` siguen en verde y que `git diff` no cambia ninguna cadena enviada
- [x] 2.2 Crear `src/Diagnostics/PageCache.php` con `detect()` estática (clase `Cache_Enabler` o constante `CACHE_ENABLER_VERSION` → `array( id, name, version )`, filtro `wpasl_diagnostics_page_cache` documentado con docblock), `label( $handler )` estática (`cache-enabler-engine` → "Cache Enabler", otro valor tal cual) y constructor `( Settings, ContentSignals )`; verificar con un test rápido en `tests/test-diagnostics.php` que `detect()` devuelve `null` en el entorno de tests y el array cuando el filtro lo devuelve
- [x] 2.3 Implementar `PageCache::headers()` (`ContentSignals::headers( true )` más `Link` de `api_catalog_link()` cuando `manifest_enabled`), `htaccess_snippet()`, `nginx_snippet()` y `cloudflare_note()` con los textos y la estructura de D3 (condición `%{CONTENT_TYPE}` en todas las líneas, `unset` + `always set` para `Content-Signal`/`Content-Usage`, `setifempty` para `X-Robots-Tag`/`Link`, `map` por cabecera en nginx, solo las cabeceras presentes, comillas escapadas); verificar con `test_snippets_follow_settings` (D6) que con `ai-train=yes`, `content_usage_header=false` y `manifest_enabled=false` solo aparece `Content-Signal`
- [x] 2.4 Implementar `PageCache::openlitespeed_snippet()` según D3: bloque `context / { allowBrowse 1  extraHeaders <<<END_extraHeaders … END_extraHeaders  rewrite { }  addDefaultCharset off  phpIniOverride { } }` con `set Content-Signal "…"`, `set Content-Usage "…"` (si está activada) y `merge Link "<home_url/.well-known/api-catalog>; rel=api-catalog"` (si el manifiesto está habilitado), nunca `X-Robots-Tag`, valores entre comillas dobles, y comentarios que indiquen el vHost Conf de CyberPanel, las Header Operations del contexto `/` en el WebAdmin, la omisión de `X-Robots-Tag`, el posible duplicado en la respuesta regenerada y el reinicio graceful; verificar con `test_openlitespeed_snippet_omits_x_robots_tag` (D6): con la configuración por defecto contiene las tres cabeceras y los textos citados y no contiene `X-Robots-Tag` en las líneas del bloque `extraHeaders` mientras `htaccess_snippet()` sí lo contiene, y con `test_snippets_follow_settings` que sin `Content-Usage` ni manifiesto solo queda `Content-Signal`
- [x] 2.5 Cablear en `src/Plugin.php` el servicio `page_cache` (`new PageCache( $settings, $this->services['signals'] )`), pasarlo a `DiagnosticsController` y de ahí a `DiagnosticsTab` (constructor con tercer parámetro); verificar que `Plugin::instance()->get( 'page_cache' )` devuelve la instancia y que la suite completa sigue en verde

## 3. Detección remota en la simulación (D2)

- [x] 3.1 Añadir `x-cache-handler` a `CrawlerProbe::HEADERS` y `'page_cache' => PageCache::detect()` al estado que devuelve `CrawlerProbe::begin()`; verificar en `test_report_detects_cache_enabler_from_x_cache_handler` que el resultado crudo de `probe->run()` conserva la cabecera y la clave
- [x] 3.2 En `src/Diagnostics/Report.php` añadir `page_cache` (string) y `page_cache_served` (bool) a `defaults()` e `infrastructure()` con la lógica de D2 (detección local del crudo o `x-cache-handler`, etiqueta con `PageCache::label()`); verificar que `test_tab_renders_report_from_previous_version_without_notices` ampliado afirma `''`/`false` para un informe antiguo
- [x] 3.3 En `Report::crawler_checks()` sustituir los mensajes de `content_signal`, `x_robots_tag` y `negotiation` por las variantes que nombran Cache Enabler cuando `$infra['page_cache']` es "Cache Enabler" (variante "served it from its page cache" si la respuesta trae `x-cache-handler`, variante "is active" si no), conservando los textos actuales en cualquier otro caso; verificar con `test_report_detects_cache_enabler_from_x_cache_handler`, `test_report_uses_local_detection_without_x_cache_handler` y `test_report_shows_other_page_cache_handler_verbatim` (D6)
- [x] 3.4 En `DiagnosticsTab::render_report()` imprimir la línea "Page cache: %s" (con el sufijo de "served at least one probed response" cuando `page_cache_served`) en la lista de infraestructura; verificar en el test 3.3 que el HTML renderizado contiene "Page cache: Cache Enabler" y que sin caché no contiene "Page cache:"

## 4. Avisos en las pestañas (D4)

- [x] 4.1 En `DiagnosticsTab::render()` imprimir, cuando `PageCache::detect()` no sea `null`, el aviso `notice notice-warning inline` con el título (nombre y versión), los dos párrafos de D4, tres `<textarea readonly class="large-text code">` (`.htaccess`, nginx y OpenLiteSpeed, cada uno con su `<h4>` y, en el de OpenLiteSpeed, la nota de que `X-Robots-Tag` no va incluido; `esc_textarea()`, sin espacio inicial) y el recordatorio de Cloudflare cuando el informe almacenado tenga `cdn === 'Cloudflare'`; verificar con `test_tab_shows_cache_enabler_notice_and_snippets` y `test_tab_hides_cache_notice_without_page_cache` (D6), incluida la presencia de `END_extraHeaders`, la ausencia de "Cloudflare" sin informe y su presencia con informe Cloudflare
- [x] 4.2 En `SignalsTab::render()` imprimir, cuando `PageCache::detect()` no sea `null`, el aviso de una frase con enlace a la pestaña Diagnóstico (`Page::SLUG`, `tab=diagnostics`); verificar con `test_signals_tab_warns_about_cache_enabler` en `tests/test-content-signals.php` (con filtro activo aparece con `tab=diagnostics`; con `__return_null` no aparece) y confirmar que `ManifestsTab` no cambia
- [x] 4.3 Revisar que todas las cadenas nuevas están en inglés con el text domain `wp-agent-support-layer`, con comentarios `translators:` donde haya placeholders, y ejecutar `vendor/bin/phpcs --no-cache src/Diagnostics src/Admin/Tabs src/Signals tests/test-diagnostics.php tests/test-content-signals.php` sin errores ni avisos

## 5. Cobertura documentada (D7)

- [x] 5.1 En `docs/spec-coverage.md` añadir en `agent-diagnostics` una fila por escenario del delta (Cache Enabler detectado por `X-Cache-Handler`, Cache Enabler activo sin cabecera, otra caché de página, sin caché de página, informe anterior sin hallazgo, aviso con Cache Enabler activo, fragmentos con configuración por defecto, fragmento para OpenLiteSpeed sin `X-Robots-Tag` → `Test_Diagnostics::test_openlitespeed_snippet_omits_x_robots_tag`, fragmentos con entrenamiento permitido y sin manifiesto, recordatorio de Cloudflare, sin Cache Enabler, detección sustituida por filtro, aviso en la pestaña Señales) apuntando a los tests de D6, y citar `detect-cache-enabler-page-cache` en el párrafo introductorio; verificar que cada escenario del delta tiene fila

## 6. Verificación final

- [x] 6.1 Ejecutar `vendor/bin/phpunit` y `vendor/bin/phpunit -c tests/multisite.xml.dist` y verificar que pasan en verde con los tests nuevos contados respecto a 1.1
- [x] 6.2 Ejecutar `vendor/bin/phpcs --no-cache` sobre todo el proyecto y verificar código de salida 0
- [x] 6.3 Ejecutar `openspec validate detect-cache-enabler-page-cache --strict` sin errores y confirmar con `git diff --stat` que `readme.txt`, `README.md`, `languages/` y la versión del plugin no cambiaron
