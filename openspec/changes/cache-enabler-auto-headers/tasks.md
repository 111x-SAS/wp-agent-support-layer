## 1. Línea base

- [ ] 1.1 Comprobar el entorno de tests (`vendor/`, `wordpress-tests-lib`, contenedor `wpasl-mysql`; si falta, `composer install`, `composer run build` y `bash bin/install-wp-tests.sh wordpress_test root root 127.0.0.1:3306 latest`) y verificar que `vendor/bin/phpunit`, `vendor/bin/phpunit -c tests/multisite.xml.dist` y `vendor/bin/phpcs --no-cache` pasan en verde antes de tocar código, anotando el número de tests de cada suite

## 2. Marcador de verificación en el bloque de `.htaccess` (D4, D5, D6)

- [ ] 2.1 En `src/Diagnostics/PageCache.php` extraer `htaccess_lines()` (array con los comentarios, `<IfModule mod_headers.c>`, las líneas actuales y `</IfModule>`) y hacer que `htaccess_snippet()` devuelva `implode( "\n", $this->htaccess_lines() ) . "\n"`; verificar que `test_tab_shows_cache_enabler_notice_and_snippets` y `test_snippets_follow_settings` siguen en verde sin cambiar sus aserciones
- [ ] 2.2 Añadir a `htaccess_lines()`, dentro del `<IfModule>`, la línea `Header always set X-WPASL-Headers "htaccess" "expr=%{CONTENT_TYPE} =~ m#^text/html#"` (constante `PageCache::MARKER_HEADER = 'X-WPASL-Headers'` y `MARKER_VALUE = 'htaccess'`), sin tocar `nginx_snippet()` ni `openlitespeed_snippet()`; verificar con aserciones nuevas en `test_tab_shows_cache_enabler_notice_and_snippets` (la línea aparece en el bloque de `.htaccess`), en `test_openlitespeed_snippet_omits_x_robots_tag` (no aparece en el bloque de OpenLiteSpeed) y en `test_snippets_follow_settings` (aparece en `htaccess_snippet()` incluso sin `Content-Usage`, `X-Robots-Tag` ni `Link`, y no aparece en los otros dos)
- [ ] 2.3 Añadir `x-wpasl-headers` a `CrawlerProbe::HEADERS` en `src/Diagnostics/CrawlerProbe.php`; verificar que `Test_Diagnostics` sigue en verde y con un test rápido que `fetch()` conserva la cabecera cuando la respuesta simulada la incluye
- [ ] 2.4 Añadir `test_marker_header_is_never_sent_by_php` en `tests/test-diagnostics.php` (o `tests/test-delivery.php` si encaja mejor): una página HTML, una URL `.md`, `robots.txt` y `llms.txt` servidos por el plugin no llevan `X-WPASL-Headers`; verificar que pasa

## 3. Servicio `HtaccessHeaders`: entorno, ruta, escribibilidad y estado (D1, D2, D3, D8, D10)

- [ ] 3.1 Crear `src/Diagnostics/HtaccessHeaders.php` con constructor `( PageCache, CrawlerProbe, Storage, Page )`, constantes `ACTION = 'wpasl_apply_htaccess'`, `NONCE = 'wpasl_apply_htaccess_nonce'`, `MARKER = 'WP Agent Support Layer'`, `BACKUP_FILE = 'system/htaccess.backup'`, `BACKUP_OPTION = 'wpasl_htaccess_backup'` y `RESULT_TRANSIENT = 'wpasl_htaccess_result_'`, y `register()` que engancha `admin_post_` . `ACTION` a `handle()`; verificar con `php -l` y `vendor/bin/phpcs --no-cache src/Diagnostics/HtaccessHeaders.php`
- [ ] 3.2 Implementar `environment()` según D2 (rama `apache_get_modules()` con `mod_headers` y versión `>= 2.4.7` o no reconocible; rama `SERVER_SOFTWARE` con Apache con o sin versión, LiteSpeed salvo `LSWS_EDITION` con `Openlitespeed`, resto no compatible) devolviendo `array{compatible, server, version, mod_headers, reason}` y pasando por el filtro documentado `wpasl_htaccess_environment`; verificar con `test_htaccess_environment_detection` que, simulando `SERVER_SOFTWARE` (`Apache/2.4.58`, `Apache/2.2.34`, `Apache`, `LiteSpeed` con y sin `LSWS_EDITION=Openlitespeed 1.7.19`, `nginx/1.24.0`, vacío) y restaurándolo en `tear_down()`, el resultado es el esperado en cada caso y que el filtro sustituye el resultado
- [ ] 3.3 Implementar `file()` (`get_home_path()` + `.htaccess`, cargando `wp-admin/includes/file.php` y `wp-admin/includes/misc.php` con `require_once` si las funciones no existen, filtro documentado `wpasl_htaccess_file`), `writable()` (regla de `save_mod_rewrite_rules()` con `wp_is_writable()`) y `available()` (`! is_multisite()`, `PageCache::detect()`, entorno compatible, escribible); verificar con `test_htaccess_availability` que con los filtros de Cache Enabler, entorno y archivo temporal `available()` es verdadero en sitio único y falso en multisitio, y que es falso con un archivo en un directorio no escribible (`chmod 0555` sobre un directorio temporal, restaurado en `tear_down()`), sin Cache Enabler o con entorno no compatible
- [ ] 3.4 Implementar `status()` según D8 (`extract_from_markers()` vacío → `not_applied`; comparación con `htaccess_lines()` ignorando líneas que empiezan por `#` y espacios de los extremos → `current` o `stale`); verificar con `test_htaccess_status` sobre el archivo temporal: sin marcadores `not_applied`, con el bloque actual (escrito con `insert_with_markers()` en el test) `current`, con el bloque y `ai-train` cambiado por `update_option` + `flush_cache()` `stale`

## 4. Copia de seguridad, escritura, verificación y restauración (D4, D6, D7)

- [ ] 4.1 Implementar `backup()` (lectura íntegra del archivo o vacío si no existe, `Storage::write( BACKUP_FILE, … )`, opción `BACKUP_OPTION` con `time`, `existed`, `bytes` y `file`, sin autoload) y `restore()` (`file_put_contents( …, LOCK_EX )` sobre la misma ruta si existía, `unlink()` si no); verificar con `test_htaccess_backup_and_restore` que tras `backup()` el archivo de copia contiene el original, que solo existe una copia tras dos llamadas, que `restore()` deja el archivo byte a byte igual al original y que con `existed => false` elimina el archivo
- [ ] 4.2 Implementar `verify()` según D6: URL de `CrawlerProbe::sample_post()` o `home_url( '/' )` con `wpasl_verify` aleatorio, `$probe->fetch( $url, <UA de las comprobaciones de sitio>, 'text/html' )`, éxito solo con estado 200, `x-wpasl-headers` conteniendo `htaccess` y `content-signal` no vacío, motivo textual en cada fallo (`request_error:…`, `http_<código>`, `redirect_<código>`, `header_missing:X-WPASL-Headers`, `header_missing:Content-Signal`); verificar con `test_htaccess_verify_request_shape` que se hace exactamente una petición, al host propio, con `Accept: text/html`, `redirection` 0 y el parámetro `wpasl_verify`, y con `test_htaccess_verify_outcomes` los cinco resultados (éxito, sin marcador, 500, `WP_Error`, 301)
- [ ] 4.3 Implementar `apply()` con la secuencia de la spec: entorno + escribible (fallo → `step => 'environment'`), `backup()` (fallo → `step => 'backup'`, sin tocar el archivo), `insert_with_markers( file(), MARKER, htaccess_lines() )` (`false` → `step => 'write'`), `verify()` (fallo → `restore()`, `step => 'verify'`, `restored => bool`), devolviendo `array{ok, step, reason, restored}`; verificar con `test_htaccess_apply_success` (copia creada, bloque entre marcadores con las líneas de `htaccess_lines()`, contenido previo intacto fuera de los marcadores, una sola petición, `ok` verdadero, `status()` `current`), `test_htaccess_apply_replaces_existing_block_in_place` (bloque previo con otro `Content-Signal` y líneas ajenas antes y después: tras aplicar, esas líneas son idénticas byte a byte, hay un solo par de marcadores y `status()` es `current`) y `test_htaccess_apply_rolls_back_on_verification_failure` (para cada fallo de 4.2, el archivo queda igual que el original byte a byte, `ok` falso, `step` `verify`, `restored` verdadero y `status()` `not_applied`)
- [ ] 4.4 Cubrir los casos límite: `test_htaccess_apply_removes_created_file_when_it_did_not_exist` (archivo temporal inexistente al inicio, verificación fallida → el archivo no existe después) y `test_htaccess_apply_aborts_when_backup_fails` (simular el fallo de `Storage::write()` haciendo no escribible el directorio `system/` de la copia; `step` `backup`, ninguna petición HTTP, archivo intacto); verificar que ambos pasan y que en multisitio `apply()` devuelve `step => 'environment'` sin escribir

## 5. Acción `admin-post` y cableado (D1, D9)

- [ ] 5.1 Implementar `handle()` (`current_user_can( Page::CAPABILITY )` con `wp_die` 403, `check_admin_referer( ACTION, NONCE )`, `apply()`, `set_transient( RESULT_TRANSIENT . get_current_user_id(), $result, 5 * MINUTE_IN_SECONDS )`, `wp_safe_redirect( add_query_arg( 'wpasl_notice', 'htaccess', $page->url( 'diagnostics' ) ) )`) y `last_result()` (lee y borra el transient); verificar con `test_htaccess_handler_requires_capability_and_nonce` (editor → `WPDieException` sin peticiones ni cambios en el archivo; administrador sin nonce → `WPDieException`) y `test_htaccess_handler_applies_and_redirects` (con nonce en `$_REQUEST`: redirección capturada con `wpasl_notice=htaccess`, transient con `ok` verdadero, archivo con el bloque)
- [ ] 5.2 Cablear en `src/Plugin.php` el servicio `htaccess_headers` (`new HtaccessHeaders( $page_cache, $probe, $storage, $page )`, `register()`), pasarlo a `DiagnosticsController` y de ahí a `DiagnosticsTab` (quinto parámetro `?HtaccessHeaders $htaccess = null`); verificar que `Plugin::instance()->get( 'htaccess_headers' )` devuelve la instancia, que `has_action( 'admin_post_wpasl_apply_htaccess' )` es verdadero y que la suite completa sigue en verde

## 6. Interfaz de la pestaña Diagnóstico (D8, D9)

- [ ] 6.1 En `DiagnosticsTab::render_page_cache_notice()`, tras el `<textarea>` del fragmento de `.htaccess` y solo cuando `$this->htaccess` no es nulo y `available()` es verdadero, imprimir un bloque `wpasl-htaccess-apply` con: la comprobación positiva («Detected: %s; mod_headers: yes/not checkable; file: %s»), el estado según `status()` («Not applied», «Applied and up to date», «Applied with different values than the current settings») y un `<form method="post" action="admin-post.php">` de primer nivel con `action` oculto, `wp_nonce_field( ACTION, NONCE )` y `submit_button( 'Apply automatically' | 'Update the block', 'secondary', 'submit', false )` seguido de una descripción («Backs up .htaccess, writes the block between WordPress markers, verifies it with a request to this site and restores the backup if the verification fails.»); verificar con `test_tab_shows_htaccess_apply_control_when_available` (texto, ruta, estado, nonce, un solo `<form` dentro del aviso, ningún `<form` anidado en toda la pestaña: `preg_match_all( '/<form\b/' )` igual al número de `</form>` y ninguna apertura entre una apertura y su cierre) y `test_tab_hides_htaccess_apply_control_when_unavailable` (sin entorno compatible, archivo no escribible o multisitio: sin `wpasl-htaccess-apply`, sin `wpasl_apply_htaccess`, los tres fragmentos presentes)
- [ ] 6.2 En la misma pestaña, cuando `wpasl_notice` sea `htaccess`, leer `last_result()` y pintar el `notice-success` o el `notice-error` de D9 (fase, motivo, «No change was kept: the previous .htaccess was restored.» o la ruta de la copia si `restored` es falso); verificar con `test_tab_shows_htaccess_result_notices` para los casos éxito, fallo de verificación restaurado y fallo con restauración fallida, y que sin transient no se pinta nada
- [ ] 6.3 Sustituir en `PageCache::notice_paragraphs()` la frase «this plugin never writes .htaccess or server configuration» por un párrafo coherente («On Apache and LiteSpeed Enterprise the plugin can apply the .htaccess block for you, after a backup and a verification request; for nginx, OpenLiteSpeed and CDN rules paste the block yourself.»), actualizar la aserción `'never writes .htaccess'` de `test_tab_shows_cache_enabler_notice_and_snippets` por el texto nuevo y añadir `assertStringNotContainsString( 'never writes .htaccess', $text )`; verificar que el test pasa
- [ ] 6.4 Revisar que todas las cadenas nuevas están en inglés con el text domain `wp-agent-support-layer`, con comentarios `translators:` donde haya placeholders y con escapado (`esc_html`, `esc_attr`, `esc_url`) en toda salida; ejecutar `vendor/bin/phpcs --no-cache src/Diagnostics src/Admin/Tabs src/Plugin.php tests/test-diagnostics.php` sin errores ni avisos

## 7. Ninguna escritura fuera de la acción

- [ ] 7.1 Añadir `test_htaccess_is_never_written_outside_the_action`: con los filtros de Cache Enabler, entorno y archivo temporal activos, guardar el contenido y el `mtime` del archivo, ejecutar `Plugin::activate()` (o el equivalente de activación usado en `tests/test-lifecycle.php`), `Settings::sanitize()` con `_tab` `general` y `signals`, `Runner::run()`, `DiagnosticsController::run()` y `render_diagnostics_tab()`, y afirmar que el archivo es idéntico, que `Storage::exists( 'system/htaccess.backup' )` es falso y que ninguna petición registrada lleva `wpasl_verify`; verificar que pasa en las dos suites
- [ ] 7.2 Añadir `test_htaccess_block_goes_stale_without_rewriting` (aplicar con éxito, cambiar `signal_ai_train` a `yes` con `update_option` + `flush_cache()`, afirmar que el archivo no cambió y que la pestaña muestra «Applied with different values» y «Update the block»); verificar que pasa

## 8. Cobertura documentada y verificación final (D12)

- [ ] 8.1 En `docs/spec-coverage.md` añadir en `agent-diagnostics` una fila por escenario del delta (marcador nunca enviado desde PHP, solo fragmentos cuando no está disponible, entorno compatible y archivo escribible, sin permisos o sin nonce, aplicación correcta, bloque ya presente, verificación sin marcador, verificación con error/tiempo/500/redirección, archivo inexistente restaurado, copia imposible, solo la copia más reciente, ninguna escritura fuera de la acción, ajustes cambiados tras aplicar, multisitio, filtros) apuntando a los tests de las secciones 2 a 7, ajustar las filas de «Aviso con Cache Enabler activo» y «Fragmentos con la configuración por defecto» al marcador, y citar `cache-enabler-auto-headers` en el párrafo introductorio; verificar que cada escenario del delta tiene fila
- [ ] 8.2 Ejecutar `vendor/bin/phpunit` y `vendor/bin/phpunit -c tests/multisite.xml.dist` y verificar que pasan en verde con los tests nuevos contados respecto a 1.1, y que `tests/` no dejó archivos temporales
- [ ] 8.3 Ejecutar `vendor/bin/phpcs --no-cache` sobre todo el proyecto y verificar código de salida 0
- [ ] 8.4 Ejecutar `openspec validate cache-enabler-auto-headers --strict` sin errores y confirmar con `git diff --stat` que `src/Storage.php`, `readme.txt`, `README.md`, `languages/` y la versión del plugin no cambiaron
