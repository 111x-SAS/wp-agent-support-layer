## 1. Línea base

- [x] 1.1 Comprobar el entorno de tests (`vendor/`, `wordpress-tests-lib`, contenedor `wpasl-mysql`; si falta, `composer install`, `composer run build` y `bash bin/install-wp-tests.sh wordpress_test root root 127.0.0.1:3306 latest`) y verificar que `vendor/bin/phpunit`, `vendor/bin/phpunit -c tests/multisite.xml.dist` y `vendor/bin/phpcs --no-cache` pasan en verde antes de tocar código, anotando el número de tests

## 2. Ajustes (D5)

- [x] 2.1 En `src/Settings.php` añadir `auth_md_enabled` (`true`) y `auth_md_notes` (`''`) a `defaults()`, ambas claves a `TAB_KEYS['manifests']`, y su saneado en `sanitize_key_value()` (`auth_md_enabled` como booleano; `auth_md_notes` con `sanitize_textarea_field()` y `mb_substr( …, 0, 4000 )`); verificar ampliando `Test_Settings::test_defaults` y `test_sanitize_is_idempotent_for_every_field`, y con tests nuevos `test_auth_md_notes_are_truncated_to_4000_chars` (5000 caracteres → 4000, segunda pasada idéntica) y `test_saving_another_tab_keeps_auth_md_settings` (guardar `general` conserva casilla y notas)

## 3. Builder y router de auth.md (D1, D3, D4)

- [x] 3.1 Crear `src/Manifest/AuthMdBuilder.php` (`ArtifactGeneratorInterface`, `id()` = `auth-md`, `FILE = 'auth.md'`, constructor `( Settings, CapabilityRegistry, ContentSignals )`, `enabled()` = `manifest_enabled && auth_md_enabled`, `generate( Storage )` que escribe o borra el archivo) y `document()` con la plantilla en inglés de D4: H1 `# {nombre} auth.md`, Audience, Registration and credential provisioning, Supported access methods con Public endpoints (capacidades del registro menos `auth-md`) y Discovery documents (`llms.txt`, catálogo, `agent-skills.json`, OpenAPI, Markdown por `.md`/`?wpasl=md` y `Accept`), Credential use, Usage policy (valores de `ContentSignals::values()` y `robots.txt`), Contact (`Settings::contact_email()`) y Notes solo con notas, pasando el resultado por el filtro `wpasl_auth_md` documentado con docblock; verificar con `Test_Auth_Md::test_document_structure`, `test_contact_email_falls_back_to_admin_email`, `test_admin_notes_section`, `test_document_never_mentions_oauth_or_application_passwords` (`stripos` de `oauth`, `agent_auth`, `application password`), `test_plain_permalinks_describe_query_arg_and_rest_route` y `test_filter_changes_the_document`
- [x] 3.2 Crear `src/Manifest/AuthMdRouter.php` calcado de `LlmsTxtRouter`: `requested( $request_uri )` estático (path base y `ManifestRouter::exact_relative_path()`, `true` solo para `auth.md`), `physical_path()` con filtro `wpasl_physical_auth_md_path` (`ABSPATH . 'auth.md'`) y `physical_file_exists()`, `handle_request()` en `parse_request` prioridad 1 (nada si no es la ruta; se aparta con archivo físico; 404 por `query_vars` cuando `enabled()` es falso; `serve()` si no), `invalidate()` en `update_option_`/`add_option_wpasl_settings` (`flush_cache()` y borrado del archivo), `document()` con generación bajo demanda, `headers()` (`text/markdown; charset=utf-8`, `X-Markdown-Tokens`, `Cache-Control: public, max-age=<Delivery::max_age()>`, `nosniff`) y `serve()` con `wpasl_before_serve` de contexto `auth-md` y `wpasl_terminate_after_serve`; verificar con `test_route_serves_auth_md_with_lazy_generation` (200, cabeceras, `Content-Signal`, sin `X-Robots-Tag`, la segunda petición no regenera), `test_route_is_404_when_disabled`, `test_route_is_404_when_manifests_disabled`, `test_physical_file_takes_precedence` y `test_non_canonical_root_paths_are_not_served`
- [x] 3.3 Cablear en `src/Plugin.php` los servicios `auth_md` (builder con `CapabilityRegistry` y `signals`) y `auth_md_router` después de `manifest_router`, registrar el builder con `$runner->add_artifact_generator()`, y documentar el contexto `auth-md` en el docblock de `ContentSignals::send()`; verificar con `test_builder_is_registered_as_artifact_generator_and_respects_the_toggle` (con la casilla activa `Runner::regenerate_artifacts()` deja `auth.md` en el almacenamiento; desactivada, lo borra), `test_settings_change_invalidates_stored_file`, y que `Test_CLI` y `Test_Runner` siguen en verde (ajustar cualquier aserción que enumere los ids de artefactos)

## 4. Ruta reservada en la entrega Markdown (D2)

- [x] 4.1 En `src/Markdown/Delivery.php` hacer que `handle_md_suffix()` devuelva sin actuar cuando `AuthMdRouter::requested( $request_uri )` sea verdadero, y que `needs_query_arg()` devuelva `true` cuando `relative_path()` del enlace permanente sea `auth`; verificar con `Test_Auth_Md::test_page_with_slug_auth_loses_the_suffix_but_keeps_accept_and_query_arg` (`/auth.md` sirve el documento del sitio con la casilla activa y 404 con ella desactivada; `/auth/` con `Accept: text/markdown`, `/auth/?wpasl=md` y `/auth/.md` sirven la página; `markdown_url()` devuelve la forma `?wpasl=md`; `llms.txt` enlaza esa forma) y `test_reserved_path_respects_base_path` (sitio bajo `/blog/`: `/blog/auth.md` es el documento, `/auth.md` no); confirmar que `Test_Delivery` y `Test_Llms_Txt::test_every_emitted_markdown_url_is_servable` siguen en verde

## 5. Descubrimiento (D6)

- [x] 5.1 En `src/Manifest/CapabilityRegistry.php` añadir la capacidad `auth-md` (nombre, descripción, `home_url( '/auth.md' )`, sin parámetros, `text/markdown`) tras `site-index` cuando `manifest_enabled && auth_md_enabled`; en `src/Manifest/ManifestBuilder.php::api_catalog()` insertar `{ href: /auth.md, type: text/markdown }` en `service-doc` entre `llms.txt` y `agent-skills.json` bajo la misma condición; en `src/Robots/RobotsTxt.php::rules_block()` añadir `# auth.md: <url>` tras la línea de `llms.txt` bajo la misma condición; verificar con `Test_Auth_Md::test_capability_catalog_and_robots_announce_auth_md` (registro, `agent_skills()`, `api_catalog()`, `rules_block()` y el `robots.txt` servido) y `test_nothing_announces_auth_md_when_disabled`, ampliando `Test_Agent_Manifest::test_api_catalog_route` y `test_default_capabilities_cover_rest_markdown_index_and_openapi` (OpenAPI sigue sin incluir `auth.md`) y confirmando que `Test_Crawler_Robots` sigue en verde

## 6. Pestaña Manifiestos (D5)

- [x] 6.1 En `src/Admin/Tabs/ManifestsTab.php` añadir `home_url( '/auth.md' )` a la lista de enlaces con su descripción, mencionar `auth.md` en el párrafo introductorio, la fila "Publish auth.md" (`wpasl_settings[auth_md_enabled]`, descripción "Requires the manifests above. When disabled, /auth.md responds 404."), la fila "auth.md notes (Markdown)" (`textarea` `wpasl_settings[auth_md_notes]`, `rows="6"`, `class="large-text code"`, `esc_textarea()`, descripción que pide no pegar credenciales) y el aviso `notice notice-warning inline` cuando `AuthMdRouter::physical_file_exists()`; verificar con `Test_Auth_Md::test_manifests_tab_renders_auth_md_fields` y la parte de aviso de `test_physical_file_takes_precedence`, manteniendo `Test_Agent_Manifest::test_manifests_tab_renders` en verde

## 7. Diagnóstico (D7)

- [x] 7.1 En `src/Diagnostics/CrawlerProbe.php::site_targets()` añadir `'auth' => home_url( '/auth.md' )` tras `llms`; en `src/Diagnostics/Report.php::site_checks()` añadir `'auth' => array( 'text/markdown', __( 'auth.md', … ) )` entre `llms` y `skills`; en `src/Admin/Tabs/DiagnosticsTab.php` añadir `/auth.md` a los archivos de descubrimiento de `curl_commands()` y `auth.md` a la frase del checklist sobre cachés; verificar con `Test_Diagnostics::test_report_checks_auth_md` (transporte simulado: 200 `text/markdown` → correcto, 404 → error, 200 `text/html` → advertencia), `test_probe_only_sends_get_to_known_targets` (todas las URLs pedidas por `probe->run()` son `GET` sobre el conjunto esperado, ninguna de registro) y la ampliación de `test_tab_renders_checklist_and_curl_commands` (`curl -s '<home_url/auth.md>'` y "auth.md" en el checklist); confirmar que `test_tab_renders_report_from_previous_version_without_notices` sigue en verde con un informe sin la clave `auth`

## 8. Calidad y cobertura (D8)

- [x] 8.1 Revisar que todas las cadenas de interfaz nuevas están en inglés con el text domain `wp-agent-support-layer` y comentarios `translators:` donde haya placeholders, que la plantilla de `auth.md` no pasa por `__()` (decisión D4), que los filtros nuevos `wpasl_physical_auth_md_path` y `wpasl_auth_md` tienen docblock, y ejecutar `vendor/bin/phpcs --no-cache src/Manifest src/Markdown/Delivery.php src/Settings.php src/Admin/Tabs src/Robots src/Diagnostics src/Plugin.php tests/test-auth-md.php tests/test-settings.php tests/test-diagnostics.php tests/test-agent-manifest.php` sin errores ni avisos
- [x] 8.2 En `docs/spec-coverage.md` citar `auth-md-discovery` en el párrafo introductorio y añadir una fila por escenario de los deltas (`agent-manifest`: publicación, documento ausente, publicación desactivada, manifiestos deshabilitados, archivo físico, ruta no canónica, slug `auth` con y sin publicación, estructura, correo, notas, sin credenciales, enlaces simples, filtro, capacidad y catálogo con y sin `auth.md`, cambio de notas, regeneración al final del ciclo; `markdown-delivery`: ruta reservada, URL alternativa con colisión, subdirectorio; `admin-settings`: defaults, guardar Manifiestos, guardar otra pestaña, notas por encima del límite, enlaces de la pestaña; `ai-crawler-robots`: puntero con y sin `auth.md`; `agent-diagnostics`: `auth.md` servido, ausente, tipo inesperado, sin peticiones de registro, comando `curl`) apuntando a los tests de las tareas 2 a 7; verificar que cada escenario de los cinco deltas tiene fila

## 9. Verificación final

- [x] 9.1 Ejecutar `vendor/bin/phpunit` y `vendor/bin/phpunit -c tests/multisite.xml.dist` y verificar que pasan en verde con los tests nuevos contados respecto a 1.1
- [x] 9.2 Ejecutar `vendor/bin/phpcs --no-cache` sobre todo el proyecto y verificar código de salida 0
- [x] 9.3 Ejecutar `openspec validate auth-md-discovery --strict` sin errores y confirmar con `git diff --stat` que `readme.txt`, `README.md`, `languages/` y la versión del plugin no cambiaron
- [x] 9.4 Comprobar de extremo a extremo en un WordPress local (o en el Docker de smoke) que `curl -s http://<sitio>/auth.md` devuelve `Content-Type: text/markdown; charset=utf-8`, un cuerpo cuyo H1 termina en `auth.md`, sin la palabra `OAuth`, y que `/.well-known/api-catalog`, `/agent-skills.json` y `/robots.txt` lo enlazan; anotar la evidencia para la release
